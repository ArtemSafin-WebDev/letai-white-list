# Letai whitelist landing

Мини-сборка лендинга на Vite, TypeScript, SCSS и Handlebars.

```bash
npm install
npm run dev
```

Базовый размер: `1rem = 10px`. Адаптив футера переключается на мобильный при ширине до `767px`.
